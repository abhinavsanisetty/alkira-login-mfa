/**
 * Application root.
 *
 * Placeholder until the router lands. The screens it will mount are Login,
 * the MFA challenge, Sign Up, and the protected connectors page.
 */
export function App() {
  return null;
}
